language: python
python:
  - "3.6"      # current default Python on Travis CI
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - PYTHONPATH=./solarhouse pytest --doctest-modules --pep8 --collect-in-virtualenv -vv -l solarhouse